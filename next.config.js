/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add your other config options here if needed
  // images: { domains: ['example.com'] },
  // etc.
};

module.exports = nextConfig;
